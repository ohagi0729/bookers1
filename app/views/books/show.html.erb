<p><span>Title:</span><%= @book.title %></p>
<p><span>Body:</span><%= @book.body %></p>

  <span>
    <%= link_to 'Edit', edit_book_path(@book.id) %>
  </span>

  <span>
    <%= link_to 'Back', "/books" %>
  </span>